# Service Catalog

The catalog records services that exist and are deployable. Catalog membership
does not imply production runtime allocation.

To consume runtime, a service must also have a production promotion under
`promotions/prod/`.
